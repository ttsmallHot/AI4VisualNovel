# AI Visual Novel Environment Configuration
# ===================================

# --- 1. Text Generation Configuration ---
# Text generation provider, currently supports "openai" or "google"
TEXT_PROVIDER=google
# Model name
MODEL=gemini-3-pro-preview

# --- 2. Image Generation Configuration ---
# Image generation provider: currently supports "openai" or "google", "google" is recommended
IMAGE_PROVIDER=google
# Image generation model, "gemini-3-pro-image-preview" is recommended
IMAGE_MODEL=gemini-3-pro-image-preview

# --- 3. API Key Configuration (Global) ---
# If you selected "openai" above, you need to fill in the OpenAI configuration
OPENAI_API_KEY=your-openai-api-key
OPENAI_BASE_URL=your-openai-api-base-url

# Google Gemini Configuration
GOOGLE_API_KEY=your-google-api-key
GOOGLE_BASE_URL=your-google-api-base-url

# --- 4. Game Generation Parameters ---
# Total number of story graph nodes; larger numbers mean richer storylines, recommended range is 10-20
GAME_TOTAL_NODES=12
# Number of game characters including the protagonist, recommended 4-6
GAME_CHARACTER_COUNT=4
# Number of plot segments per node; larger numbers mean more detailed plot, recommended 2-5
PLOT_SEGMENTS_PER_NODE=3